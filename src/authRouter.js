//import react, router, firebase auth
//will be separated into component and container
//container:
// mapStateToProps pending and loggedIn state
// mapDispatchToProps when component will mount dispatch firebase auth state change action
//component: 
//if !loggedIn: redirect to login
//else redirect to where it needs to go