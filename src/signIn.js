// render firebase authentication component
// set signin as true in dispatch actions
// set state as prop to logged if successful.
// can also pass back to original path to support automatic redirect
// set userID in the store
// dispatch actions to retrieve firebase todo bids data